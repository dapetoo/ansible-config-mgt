# ServiceAccount for the namespaced SecretStore.
# Lives in the litellm namespace — only ExternalSecrets in litellm can use it.
# Bound to Vault role 'litellm' by seed-secrets.sh.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-vault-auth
  namespace: litellm
